{
  "rules": {
    ".read": "auth != null",
    ".write": "auth != null",
    "reservations": {
      "$reserveId": {
        ".read": "auth != null",
        ".write": "auth != null"
      }
    }
  }
}